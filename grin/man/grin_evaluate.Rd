% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterion.R
\name{grin_evaluate}
\alias{grin_evaluate}
\title{Evaluate a stopping criterion against an inference result}
\usage{
grin_evaluate(criterion, result, constructs = NULL)
}
\arguments{
\item{criterion}{A \code{grin_criterion} (see \code{\link[=grin_criterion]{grin_criterion()}}).}

\item{result}{A \code{grin_result} (e.g. \code{grin_infer(...)$result}).}

\item{constructs}{Optional constructs list (e.g. \code{grin_infer(...)$constructs});
required if any target in \code{criterion} is a probability target.}
}
\value{
A \code{grin_decision}: \verb{$stop} (logical), \verb{$checks} (list of per-target detail),
\verb{$combine}, \verb{$blocked_by} (names of targets whose evidence flag says the data
cannot decide them -- these will never be met, no matter how much data you add).
}
\description{
Evaluate a stopping criterion against an inference result
}
\examples{
\donttest{
M <- matrix(c(71, 17,  9,  5,
              20, 67,  5,  9,
              13,  6, 63, 20,
               5, 10, 15, 71), nrow = 4, byrow = TRUE)
out <- grin_infer(M)
crit <- grin_criterion(list(
  grin_target_precision(params = c("zx", "zy"), sd_max = 0.10),
  grin_target_probability("PS_A", at_least = 0.90)
), combine = "any")
decision <- grin_evaluate(crit, out$result, out$constructs)
print(decision)
}
}
