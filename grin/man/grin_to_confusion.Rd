% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{grin_to_confusion}
\alias{grin_to_confusion}
\title{Normalise any supported input into a canonical-order confusion matrix}
\usage{
grin_to_confusion(
  data,
  stim_labels = NULL,
  resp_labels = NULL,
  factor_a = NULL,
  factor_b = NULL,
  order = NULL,
  trials = NULL,
  sep = "/",
  long = FALSE
)
}
\arguments{
\item{data}{A 4x4 matrix/data.frame, a length-16 vector (read row-major: stimulus
varies slower than response), or (with \code{long = TRUE}) a long-format data.frame
with \code{stimulus} and \code{response} columns (plus an optional \code{count} column).}

\item{stim_labels, resp_labels}{Character vectors of the 4 row/column labels, in
the order \code{data} actually has them. If labels are canonical A1B1-style tokens
they are matched directly; otherwise supply \code{factor_a}/\code{factor_b} too.}

\item{factor_a, factor_b}{Length-2 character vectors giving the two levels of each
dimension, e.g. \code{c("Old","Young")}, \code{c("Neg","Pos")}, needed to place non-canonical
labels like \code{"Old/Neg"}.}

\item{order}{Pass \code{"canonical"} to assert \code{data} is already in canonical order
(skips label resolution entirely). A bare, unlabelled 4x4 with neither \code{order}
nor labels is refused rather than guessed.}

\item{trials}{Optional per-stimulus trial totals (length 4); required if \code{data}
looks like proportions rather than counts. Defaults to row sums of the counts.}

\item{sep}{Separator used to split single-string labels like \code{"Old/Neg"}.}

\item{long}{If \code{TRUE} (or \code{data} is a data.frame with a \code{stimulus} column),
treat \code{data} as long-format trial-level or aggregated data.}
}
\value{
A \code{grin_confusion_input} object: \verb{$counts} (4x4 integer matrix), \verb{$trials}
(length-4 integer), \verb{$placement}, \verb{$warnings}, \verb{$asserted_order}.
}
\description{
Coerces a confusion matrix in (almost) any shape and label scheme into the
canonical-order (A1B1, A1B2, A2B1, A2B2 x A1B1, A1B2, A2B1, A2B2) form GRIN's
models expect, and never guesses the two things a wrong guess would silently
corrupt: stimulus/response order, and counts-vs-proportions.
}
