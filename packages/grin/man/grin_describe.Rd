% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{grin_describe}
\alias{grin_describe}
\title{Describe how input would be parsed, without running the network}
\usage{
grin_describe(data, printout = TRUE, ...)
}
\arguments{
\item{data}{A 4x4 matrix/data.frame, a length-16 vector (read row-major: stimulus
varies slower than response), or (with \code{long = TRUE}) a long-format data.frame
with \code{stimulus} and \code{response} columns (plus an optional \code{count} column).}

\item{printout}{Print a human-readable report (default \code{TRUE}).}

\item{...}{Passed on to \code{\link[=grin_to_confusion]{grin_to_confusion()}} (\code{stim_labels}, \code{factor_a}, etc.).}
}
\value{
Invisibly, a list report (\verb{$ready}, \verb{$errors}, \verb{$warnings}, \verb{$counts},
\verb{$trials}, \verb{$placement}, \verb{$asserted_order}).
}
\description{
Setup guide / dev mode / exception surface in one. Never raises: parse errors are
captured and printed, not thrown, so this is safe to run while wiring up an
experiment. Pass the same arguments you would pass to \code{\link[=grin_to_confusion]{grin_to_confusion()}}.
}
