% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-individual.R
\name{grin_plot_constructs}
\alias{grin_plot_constructs}
\title{Plot one participant's construct probabilities}
\usage{
grin_plot_constructs(result, constructs, color = NULL)
}
\arguments{
\item{result}{A \code{grin_result}.}

\item{constructs}{The paired constructs list (e.g. \code{grin_infer(M)$constructs}).}

\item{color}{Use the house colour style instead of black-on-white? Default
\code{NULL} defers to \code{options(grin.color)}.}
}
\value{
A ggplot object.
}
\description{
Two panels: correlation structure (P(PI), P(RHO1), P(free), which sum to 1)
and separability (P(separable A), P(separable B), each independent). Bars
for a construct the data cannot decide (\verb{evidence_* == FALSE}) are flagged
rather than plotted as if they were informative -- the identifiability
limit is a property of the data, and this plot says so rather than hiding it.
}
\examples{
\donttest{
M <- matrix(c(71, 17,  9,  5, 20, 67,  5,  9,
              13,  6, 63, 20,  5, 10, 15, 71), nrow = 4, byrow = TRUE)
out <- grin_infer(M)
grin_plot_constructs(out$result, out$constructs)
}
}
