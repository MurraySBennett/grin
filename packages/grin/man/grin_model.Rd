% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{grin_model}
\alias{grin_model}
\title{Load (and cache) a GRIN TorchScript model}
\usage{
grin_model(path = NULL)
}
\arguments{
\item{path}{Path to a \code{.pt} TorchScript export. Defaults to the model bundled
with this package.}
}
\value{
A \code{grin_model} object, ready to pass to \code{\link[=grin_infer]{grin_infer()}}. Models are
cached per session by path, so repeated calls are free.
}
\description{
Requires the \pkg{torch} package (CRAN; downloads libtorch on first use via
\code{torch::install_torch()}). No Python is used anywhere in this path.
}
\examples{
\donttest{
# Inference needs libtorch, which is downloaded on first use and is not
# present on CRAN's check machines; the guard keeps this example safe there.
if (torch::torch_is_installed()) {
  M <- matrix(c(71, 17,  9,  5, 20, 67,  5,  9,
                13,  6, 63, 20,  5, 10, 15, 71), nrow = 4, byrow = TRUE)
  model <- grin_model()
  print(model)
}
}
}
