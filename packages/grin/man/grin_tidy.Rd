% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{grin_tidy}
\alias{grin_tidy}
\title{Tidy one or many GRIN inferences into a data.frame}
\usage{
grin_tidy(results, ids = NULL)
}
\arguments{
\item{results}{A single \code{grin_inference} (from \code{\link[=grin_infer]{grin_infer()}}), or an
(optionally named) list of them -- e.g. from looping \code{grin_infer()} over
a sample of participants.}

\item{ids}{Optional character vector of participant IDs, same length as
\code{results}. Defaults to the list's names if present, otherwise \verb{p1, p2, ...}.}
}
\value{
A data.frame, one row per participant: \code{id}, \code{model_class}, the 12
parameter estimates (\code{zx_0}...\code{rho_3}), their SDs (\code{zx_0_sd}...\code{rho_3_sd}),
\code{p_PI}/\code{p_sep_A}/\code{p_sep_B}, \code{evidence_PI}/\code{evidence_sep_A}/\code{evidence_sep_B},
and \code{x_bias}/\code{y_bias} (the decision-criterion bias of \code{\link[=grin_response_bias]{grin_response_bias()}}).
}
\description{
Tidy one or many GRIN inferences into a data.frame
}
\examples{
\donttest{
M <- matrix(c(71, 17,  9,  5, 20, 67,  5,  9,
              13,  6, 63, 20,  5, 10, 15, 71), nrow = 4, byrow = TRUE)
out <- list(p1 = grin_infer(M), p2 = grin_infer(M))
grin_tidy(out)
}
}
